import './bootstrap';

import AOS from 'aos';
import 'aos/dist/aos.css';

AOS.init({
  duration: 1000, // lama animasi (ms)
  offset: 120,    // jarak sebelum animasi dimulai
  delay: 100,     // jeda animasi (ms)
});
